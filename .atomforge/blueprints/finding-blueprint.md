# Findings — atom-{{NN}}-{{short-name}}

- Atom: `docs/atoms/phase-{{NN}}/atom-{{NN}}-{{short-name}}.md`
- Append-only: new review/validation runs ADD sections; never delete or rewrite old findings.

> Severity: **HIGH** = correctness/security/data-loss — blocks `validate` and `commit` while
> OPEN. **MEDIUM** = should fix before phase ends. **LOW** = polish/improvement.
> Status: `OPEN` → (`APPROVED` → `FIXED`) | `WONT-FIX <reason>`.

## Run: {{DATE}} — source: review | validate

| ID | Severity | Finding (with evidence: file:line) | Suggested fix | Status |
|---|---|---|---|---|
| F-{{NN}}.1 | HIGH | … | … | OPEN |
| F-{{NN}}.2 | MEDIUM | … | … | OPEN |
| F-{{NN}}.3 | LOW | … | … | OPEN |

Notes: {{anything that isn't a discrete finding — observations, deferred questions}}
