# Session log — 2026-07-17 (resume → all 17 atoms COMMITTED)

## What happened

1. **atom-04 finished** — root cause of the failing cascade test: `build_engine` never set
   `PRAGMA foreign_keys=ON` on SQLite, so `ON DELETE CASCADE` + `passive_deletes=True`
   no-opped. Fixed with a sqlite-only connect listener. `8e85e3f`.
2. **atoms 05–06** implemented/validated/committed (probe-helper `r.path` →
   `getattr(r, "path", None)` for newer FastAPI). `7ac7623`, `58ef9c5`.
3. **atoms 07–10** (phase-03) review-atom → implement → validate → commit.
   Zero code deviations. `a2ca1e7`, `cd0f4aa`, `a0265cf`, `1614835`.
4. **atoms 11–13** (phase-04). atom-11: two test corrections (wrong RRF math expectation;
   dim=8 fake-embedder tie — verified numerically, tests now use dim=32). atom-12: fake LLM
   source detection keyed on '[1] (' instead of the always-present 'Sources:' header.
   `0194e11`, `b14613e`, `10b834b`.
5. **atoms 14–17** (phase-05). Watcher validated live against the API (2 files → INDEXED,
   re-scan skips). Frontend: test script fixed for Node 22 (`--test tests/*.test.ts`);
   StatTile prop widened for exactOptionalPropertyTypes. Full oracle green
   (typecheck + 12 node tests + build with /login /chat /admin). `7130d38`, `27dbd44`,
   `f0dacb1`, `ea61725`.

## Final verification (this session, Linux sandbox, Python 3.10 / Node 22)

- backend: **61 passed**; watcher: **7 passed**; frontend: typecheck clean, 12/12, build clean.
- alembic upgrade head → downgrade base → upgrade head, all clean (sqlite oracle).
- Full-stack e2e smoke: bootstrap login → upload policy.txt → INDEXED, 1 vector →
  chat stream returns session/token*/sources/done with [1] citation to policy.txt.
- README extended with frontend/watcher/production sections; `.env.example` gained
  RAG_UPLOAD_DIR.

## Still environment-dependent (needs Docker, not available in this sandbox)

- Real-PG migration oracle: `docker compose up -d postgres` →
  `RAG_DATABASE_URL=postgresql+asyncpg://... alembic upgrade head && downgrade base`.
- PG tsquery retrieval leg + GIN EXPLAIN check (atom-11 verification 2).
- Milvus `ensure_ready` collection creation check (atom-08 verification 2).

## State

All plans READY, atoms 01–17 **COMMITTED**, suite green, branch
`feat/phase-01-foundation`. Next: run the compose-stack checks above, then merge to main.
