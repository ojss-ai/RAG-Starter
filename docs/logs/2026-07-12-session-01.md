# Session log — 2026-07-12 (pipeline bootstrap → all atoms authored)

## Where the project stands

| Stage | State |
|---|---|
| SRS (`docs/srs.md`) | READY — FR-1…20 (FR-14…20 gap-filled: auth, RBAC, API keys, audit, validation, rate limits, ops), NFR-1…8 |
| Stack & mode | ADR-0001 — fastapi/postgres/milvus/nextjs; mode `normal`; testRunner `cd backend && python -m pytest -q` |
| Plans phase-01…05 | all READY |
| **atom-01…03** (phase-01 foundation) | **COMMITTED** — backend scaffold, schema+alembic, compose/Makefile. Compose hardening fix-commits after automated security review (loopback ports, env credentials). |
| **atom-04** (auth core) | READY, **extraction committed as WIP** — 16/17 tests pass; see "known issue" below |
| atom-05…06 (RBAC/keys, rate-limit/audit) | READY — docs only, not yet implemented |
| atom-07…17 (ingestion, chat, watcher, frontend) | DRAFT — full code in docs, awaiting review-atom → implement |
| Branch | `feat/phase-01-foundation` (pushed) · remote `https://github.com/ojss-ai/RAG-Starter` |

## Known issue (first thing to fix on resume)

`tests/test_schema.py::test_chunk_cascade_delete` FAILS (chunks not cascade-deleted,
`assert 3 == 0`). It passed at atom-02 time and now fails **even in isolation**. Suspects:
SQLite `PRAGMA foreign_keys=ON` listener in `build_engine` vs `passive_deletes=True` on the
`Document.chunks` relationship; possibly the pragma connection event never firing on the
aiosqlite StaticPool connection. Debug: check `PRAGMA foreign_keys` value inside a test
session; try `passive_deletes=False` (ORM-level cascade) as a cross-check.

## Resume runbook

1. `status` — reload state from disk.
2. Fix the cascade test → finish atom-04 (validate, commit `feat(auth): … [atom-04]`).
3. `review-atom atom-05` → implement → … → commit. Same for atom-06.
4. Real-PG migration oracle (pending, Docker Desktop was still starting):
   `docker compose up -d postgres` then in `backend/`:
   `RAG_DATABASE_URL=postgresql+asyncpg://rag:rag-dev-only@localhost:5432/rag alembic upgrade head`
   and `... downgrade base` — both must run clean (postgres skill).
5. Phases 03–05: per atom `review-atom` (certify vs the then-current codebase) → implement
   (extract `file=` blocks → run tests → fix → commit). Extraction helper used so far:
   scratchpad `extract_atom.py` (regex on ```lang file=path fences); recreate if the
   scratchpad is gone — 15 lines, documented in the atoms' RULES line.
   Atoms 10/12/13 ship `schemas_atomNN_append.py` helper blocks — MERGE into
   `app/schemas.py` and delete the helper file (instructions inline).
6. Frontend atoms (15–17): `npm install` in `frontend/`, oracle =
   `npm run typecheck && npm test && npm run build`.
7. Watcher atom (14): own venv or reuse backend's; `cd watcher && python -m pytest -q`.
8. Merge `feat/phase-01-foundation` → main when phase-01+02 are fully green, or keep
   per-phase branches per the git skill.

## Notes

- Commits so far are authored as `Thapaliya, Suraj <SThapaliya@grovergaming.com>` (work
  identity) on a personal-org remote; includeIf routing applies only to commits made after
  the remote was added. Rewriting history is the owner's call.
- Backend venv: `backend/.venv` (Python 3.14) — deps installed incl. `email-validator`.
- Two `wip`/`fix` commits deviate from the strict one-atom-one-commit rule (user-directed
  push-everything checkpoint); history is otherwise atom-tagged.
